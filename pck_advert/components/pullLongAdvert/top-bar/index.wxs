var ciphertext = (function(text) {
    if (text.length <= 2) {
        return (text.slice(0, 1) + '***')
    };
    return (text.slice(0, 2) + '***')
});
module.exports = ({
    ciphertext: ciphertext,
});